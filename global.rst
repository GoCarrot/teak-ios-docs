.. raw:: html

    <script type="text/javascript">
      var url = new URL(window.location);
      if (url.hostname.startsWith('teak-ios')) {
        url.hostname = url.hostname.replace('teak-ios', 'teak');
        url.pathname = '/projects/ios' + url.pathname;
        window.location.replace(url.href);
      }
    </script>
